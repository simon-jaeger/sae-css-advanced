{
  "devDependencies": {
    "prettier": "3.2.5",
    "browser-sync": "3.0.2"
  },
  "scripts": {
    "serve": "browser-sync start --server --directory --no-notify --watch --port 4000"
  },
  "prettier": {
    "htmlWhitespaceSensitivity": "ignore",
    "semi": false
  }
}
